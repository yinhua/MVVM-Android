<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <data class="LoginDataBinding">
       <variable
           name="loginViewModel"
           type="com.mvvm.android.viewmodel.LoginViewModel"/>
    </data>
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="com.mvvm.android.view.activity.LoginActivity">
        <EditText
            android:id="@+id/text_userName"
            android:layout_width="match_parent"
            bindEditChanged="@{loginViewModel.bindUserName}"
            android:layout_height="50dp"/>
        <EditText
            android:layout_width="match_parent"
            bindEditChanged="@{loginViewModel.bindPassword}"
            android:layout_height="50dp"/>
        <Button
            android:layout_width="match_parent"
            android:text="登录"
            android:onClick="@{loginViewModel.mOnClickLoginListener}"
            android:layout_height="50dp"/>
    </LinearLayout>
</layout>
